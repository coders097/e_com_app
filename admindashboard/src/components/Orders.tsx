import React from 'react';

const Orders = () => {
    return (
        <section className="Orders">
            <h2>My Orders</h2>
            <div className="order-item">
            {/* status:{
        type: String,
        enum : ['process','success','cancel'],
        default:'process'
    },
    products:[{
        type:mongoose.SchemaTypes.ObjectId,
        ref:"Product"
    }],
    productNos:[{
        type:Number
    }],
    totalPrice:{
        type:Number,
        required:true
    }????????????? */}
                // show his product
                // and price of his share
            </div>
        </section>
    );
};

export default Orders;